# Deployment target

For this repository, a request to publish, deploy, or release means deployment to the user's cloud server using this project's Docker Compose and Caddy configuration. Do not publish to OpenAI Sites unless the user explicitly requests OpenAI Sites.
